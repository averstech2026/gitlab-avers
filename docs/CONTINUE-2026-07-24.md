# Продолжение — gitlab-avers / VPS (2026-07-24)

Сервер: `/opt/gitlab` на `170.168.10.152` (git.averstech.ru).

## Сервисы

| Сервис | Назначение |
|--------|------------|
| `gitlab` | https://git.averstech.ru (+ location `/mfc/`) |
| `mfc-fast-create` | FastAPI MFC → https://git.averstech.ru/mfc/ |
| `planka-bridge` | Planka ↔ GitLab Issues |
| `docs` | статика шпаргалки :8081 |

## MFC

Секреты: `mfc-fast-create/.env` (gitignore).  
После правок кода: `docker compose up -d --build mfc-fast-create`.  
Смена nginx omnibus: `docker compose up -d gitlab` (долгий старт).  
Health: `curl -sS https://git.averstech.ru/mfc/api/mfc/health`

Кросс-контекст: okdesk-score-rules `docs/26`, ShiftPlanner `docs/22`.
